# mcmtastsswa

Static Web App content for hosting the `mc10.caldas.pt` MTA-STS policy.

The required policy file is published at:

```text
/.well-known/mta-sts.txt
```

Deploy the repository root as the Azure Static Web Apps application location with no build output directory.
